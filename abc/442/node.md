# C 問題
3人を選ぶ組み合わせ
(n * n-1 * n-2) / 3 * 2 * 1

# D 問題
シンプルな累積和
数列 1 2 3 4 5 について、累積和は 0 1 3 6 10 15

3 と 4 を入れ替えたら、累積和は 0 1 3 7 10 15
3 番目の値が -1
一般化すると、A の x と x+1 番目の値を入れ替えると、累積和の x+1 番目の値に A_x+1 - A_x の値が足される

# E 問題
角度をなんとかして扱いたいが、B 問題で失敗したように、座標の割り算では正確にもとまらない。

sin